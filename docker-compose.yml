services:
  nim-router:
    build: .
    image: nim-router:latest
    container_name: nim-router
    ports:
      - "8000:8000"
    volumes:
      - nim-router-data:/data
    restart: unless-stopped

volumes:
  nim-router-data:
